# برنامه درسی دوره برنامه‌نویسی پایتون برای مهندسی برق

## ۱. مبانی پایتون (پیشرفت فعلی)
*موضوعاتی که قبلاً پوشش داده شده یا در حال پیشرفت است:*
- **سینتکس پایه و متغیرها**: اعداد صحیح، اعشاری، رشته‌ها، بولین.
- **عملگرها**: حسابی، مقایسه‌ای، منطقی، و **عملگرهای بیتی** (حیاتی برای سیستم‌های نهفته/مهندسی برق).
- **ساختارهای داده**: لیست‌ها، تاپل‌ها، ست‌ها، دیکشنری‌ها.
- **کنترل جریان**: `if/else`، حلقه‌های `for`، حلقه‌های `while`، `match-case`.
- **توابع**: تعریف توابع، آرگومان‌ها، مقادیر بازگشتی، توابع لامبدا، بازگشتی.
- **ماژول‌ها و پکیج‌ها**: ایمپورت `math`، `random`، و ساخت ماژول‌های سفارشی (پوشش داده شده در جلسه ۰۵).
- **کار با فایل**: خواندن/نوشتن فایل‌های متنی (با `open` و `with`).
- **برنامه‌نویسی شی‌گرا (OOP)**: کلاس‌ها، اشیاء، ویژگی‌ها، متدها، وراثت.
- **مدیریت خطا**: `try`، `except`، `finally`، `raise`، استثناهای سفارشی.
- **قالب‌بندی رشته**: عملگر `%`، `.format()`، و **f-strings**.
- **تکرار پیشرفته**: برش‌زنی `[start:stop:step]`، اشیاء قابل تکرار، تکرارکننده‌ها، و **مولدها** (`yield`).
- **مدیریت پکیج**: استفاده از `pip` برای نصب پکیج‌ها (پوشش داده شده در جلسه ۰۵).

## ۲. محاسبات عددی با NumPy (ضروری برای مهندسی برق)
*جایگزین MATLAB برای بسیاری از کارها.*
- **آرایه‌ها در مقابل لیست‌ها**: تفاوت‌های عملکرد و قابلیت.
- **ساخت آرایه**: `linspace`، `arange`، `zeros`، `ones` (ساخت بردارهای زمانی برای سیگنال‌ها).
- **عملیات برداری**: محاسبات عنصر به عنصر بدون حلقه.
- **جبر خطی**: ضرب ماتریس (`@`)، معکوس، مقادیر/بردارهای ویژه (تحلیل مدار، سیستم‌های کنترل).
- **Broadcasting**: مدیریت آرایه‌ها با اشکال مختلف.

## ۳. نمایش داده با Matplotlib
*نمایش سیگنال‌ها و داده‌های آزمایشگاه.*
- **رسم پایه**: نمودار خطی برای سیگنال‌ها ($v(t)$ در مقابل $t$).
- **قالب‌بندی**: عنوان‌ها، برچسب‌ها، راهنما، شبکه (ساخت نمودارهای آماده انتشار).
- **زیرنمودارها**: مقایسه سیگنال‌های ورودی و خروجی.
- **نمودارهای پیشرفته**: نمودار پراکندگی، هیستوگرام، و رسم سه‌بعدی (میدان‌های الکترومغناطیسی).

## ۴. محاسبات علمی با SciPy
*جعبه ابزار برای کاربردهای مهندسی.*
- **پردازش سیگنال (`scipy.signal`)**:
    - کانولوشن.
    - فیلترینگ (پایین‌گذر، بالاگذر).
    - تبدیل فوریه (FFT) و تحلیل دامنه فرکانس.
- **بهینه‌سازی (`scipy.optimize`)**: برازش منحنی داده‌های آزمایشگاه.
- **انتگرال‌گیری (`scipy.integrate`)**: حل معادلات دیفرانسیل (مدارهای RLC).

## ۵. ریاضیات نمادین با SymPy
*حل تحلیلی برای مدارها و سیستم‌ها.*
- **متغیرهای نمادین**: تعریف $x, y, t$.
- **حل معادله**: حل معادلات KVL/KCL به صورت نمادین.
- **حساب دیفرانسیل و انتگرال**: مشتق و انتگرال.
- **تبدیل‌ها**: تبدیل لاپلاس (سیستم‌های کنترل) و لاپلاس معکوس.

## ۶. رابط‌کاری با سخت‌افزار و اتوماسیون (اختیاری/پیشرفته)
*اتصال پایتون به دنیای واقعی.*
- **ارتباط سریال (`pyserial`)**: خواندن داده سنسور از Arduino/STM32.
- **ثبت داده**: ذخیره داده‌های لحظه‌ای در فایل‌های CSV/متنی.
- **اتوماسیون**: خودکارسازی تجهیزات تست (در صورت امکان از طریق VISA/PyVISA).

## ۷. مدیریت داده با Pandas
*پردازش اندازه‌گیری‌های آزمایشگاه.*
- **دیتافریم‌ها**: خواندن فایل‌های CSV/Excel از اسیلوسکوپ یا دستگاه‌های ثبت داده.
- **پاکسازی داده**: مدیریت داده‌های گمشده یا نویز.
- **آمار**: میانگین، انحراف معیار، همبستگی خطاهای اندازه‌گیری.

## پروژه‌های پیشنهادی
۱.  **شبیه‌ساز مدار RLC**: رسم پاسخ پله با استفاده از `scipy.integrate`.
۲.  **آنالیزگر طیف صوتی**: ضبط صدا و رسم FFT با استفاده از `numpy` و `matplotlib`.
۳.  **ثبت‌کننده داده**: خواندن دما از میکروکنترلر از طریق سریال و رسم لحظه‌ای.

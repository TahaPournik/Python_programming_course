# پروژه ۲: سیستم هوشمند تغذیه (FoodService)

## 🎯 هدف پروژه
شما مدیر سلف سرویس دانشگاه هستید. سیستم شما باید فرآیند تعریف غذا، رزرو توسط دانشجو، مدیریت موجودی و تولید آمار برای آشپزخانه را انجام دهد. تمرکز اصلی بر روی **مدیریت سفارشات** و **تراکنش‌های مالی** است.

---

## 📂 ۱. ساختار فایل‌ها (اجباری)
```text
Project_2_FoodService/
├── data/
│   ├── login.csv           # اطلاعات ورود (مشترک با استاندارد کل سیستم)
│   ├── foods.csv           # منوی غذا (نام، قیمت، روز ارائه، ظرفیت)
│   └── students/           # پوشه سابقه سفارشات دانشجویان
│       ├── 4016113001.csv  # لیست سفارشات دانشجو ۱
│       └── ...
├── models/
│   ├── food.py             # کلاس غذا
│   ├── order.py            # کلاس سفارش
│   └── menu.py             # کلاس مدیریت منو
├── analysis/
│   └── sales_report.py     # تحلیل فروش
├── reports/                # نمودارهای فروش
└── main.py                 # فایل اجرایی
```

---

## 📝 ۲. شرح وظایف (Tasks)

### 🔹 مرحله اول: مدل‌سازی (OOP)
کلاس‌های زیر را طراحی کنید:
1.  **Food**: ویژگی‌ها: نام غذا، قیمت، روز هفته (شنبه تا پنج‌شنبه)،وعده(صبحانه/ناهار/شام)، موجودی باقی‌مانده.
2.  **Order**: ویژگی‌ها: شماره دانشجویی، نام غذا، تاریخ ثبت، وضعیت (رزرو شده/لغو شده).

### 🔹 مرحله دوم: مدیریت منو و داده‌ها
- فایل `foods.csv` «منوی اصلی» شماست. آشپز می‌تواند در آن تغییر ایجاد کند.
- سابقه سفارش هر دانشجو باید در فایل اختصاصی خودش (`data/students/{id}.csv`) ذخیره شود تا تاریخچه حفظ شود.

### 🔹 مرحله سوم: منطق و دسترسی‌ها (RBAC)
سیستم دو نقش اصلی دارد:
1.  **Chef (آشپز/مدیر سلف):** مسئول مدیریت غذاها و دیدن آمار.
2.  **Stud (دانشجو):** مشتری سیستم که غذا رزرو می‌کند.

### 🔹 مرحله چهارم: طراحی منو (CLI Menu)

#### 👨‍🍳 پنل آشپز (Chef Services)
1.  **مدیریت منو:** افزودن غذای جدید، حذف غذا، یا تغییر قیمت و ظرفیت.
2.  **لیست روزانه:** مشاهده لیست غذاهایی که باید برای «فردا» پخته شود (به تفکیک تعداد).
3.  **تحلیل فروش:** رسم نمودار دایره‌ای (Pie Chart) از محبوب‌ترین غذاها.
4.  **گزارش مالی:** محاسبه درآمد کل سلف در هفته جاری.

#### 🍱 پنل دانشجو (Student Services)
1.  **رزرو غذا:** مشاهده منوی هفته آینده و رزرو غذا (در صورت داشتن ظرفیت).
2.  **مدیریت سفارش:** مشاهده لیست غذاهای رزرو شده و قابلیت **لغو سفارش** (تا ۲۴ ساعت قبل).
3.  **شارژ حساب:** افزایش اعتبار ریالی (اگر سیستم مالی مستقل دارید).

### 🔹 مرحله پنجم: ارتباط با سایر سیستم‌ها (Integration) - *امتیازی*
1.  **بررسی دانشجو (از پروژه ۱):**
    - شما نباید لیست دانشجویان را خودتان داشته باشید.
    - باید از ماژول `integration` پروژه ۱ استفاده کنید تا چک کنید آیا دانشجویی با این شماره وجود دارد و فعال است؟
2.  **پرداخت (به پروژه ۴) - *اختیاری*:**
    - اگر پروژه ۴ (امور مالی) تابعی برای پرداخت دارد، باید هنگام رزرو غذا، آن تابع را صدا بزنید تا مبلغ از حساب دانشجو کسر شود: `Project_4.integration.process_payment(student_id, amount)`.

---

## 💡 راهنمایی‌های فنی
- هنگام لغو سفارش، مبلغ باید به حساب دانشجو برگردد (اگر سیستم مالی دارید) و ظرفیت غذا دوباره زیاد شود.
- برای تحلیل محبوبیت غذا، باید تمام فایل‌های `students/*.csv` را پیمایش کنید و تعداد تکرار هر غذا را بشمارید.

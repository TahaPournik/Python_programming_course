# پروژه ۴: سیستم مالی و منابع انسانی (HR & Payroll)

## 🎯 هدف پروژه
شما **حسابدار کل دانشگاه** هستید. این سیستم دو وظیفه بزرگ دارد:
1.  مدیریت حقوق و دستمزد کارمندان و اساتید.
2.  مدیریت تراکنش‌های مالی دانشجویان (کیف پول مرکزی).
این پروژه نقش "بانک" را در اکوسیستم دانشگاه بازی می‌کند.

---

## 📂 ۱. ساختار فایل‌ها (اجباری)
```text
Project_4_HR_Payroll/
├── data/
│   ├── login.csv           # اطلاعات ورود
│   ├── employees.csv       # لیست کارمندان و قراردادها
│   ├── transactions.csv    # دفتر کل تراکنش‌های مالی دانشگاه
│   ├── details/            # پوشه فیش‌های حقوقی کارمندان
│   │   ├── emp_101.csv
│   │   └── ...
│   └── summary.csv         # خلاصه وضعیت مالی ماهانه
├── models/
│   ├── employee.py         # کلاس کارمند
│   ├── payslip.py          # کلاس فیش حقوقی
│   └── transaction.py      # کلاس تراکنش
├── analysis/
│   ├── tax_calculator.py   # محاسبات مالیات
│   └── integration.py      # ماژول خدمات بانکی به سایر گروه‌ها
├── reports/                # نمودارهای مالی
└── main.py                 # فایل اجرایی
```

---

## 📝 ۲. شرح وظایف (Tasks)

### 🔹 مرحله اول: مدل‌سازی و حقوق (HR Logic)
- **Employee**: نام، کد پرسنلی، حقوق پایه ساعتی، ساعات کارکرد.
- **قوانین حقوق:**
  - **اضافه کار:** ساعات بالای ۱۶۰ ساعت در ماه با ضریب ۱.۵ محاسبه شود.
  - **مالیات:** فرمول پلکانی (مثلاً زیر ۵ میلیون معاف، بالای ۵ میلیون ۱۰٪ مالیات بر مازاد).
  - مالیات باید به صورت هوشمند محاسبه شود (اگر بازه زمانی کمتر از یک ماه است، مالیات باید به نسبت تعمیم داده شود).

### 🔹 مرحله دوم: مدیریت داده‌های مالی
- پوشه `details/` حکم زونکن‌های حسابداری را دارد. برای هر کارمند فایل‌های CSV بسازید که ریز کارکرد هر ماه در آن باشد.
- فایل `summary.csv` باید گزارش مدیریتی باشد (کل پرداختی دانشگاه در هر ماه چقدر بوده است؟).

### 🔹 مرحله سوم: طراحی منو (CLI Menu)

#### 💼 پنل ادمین مالی (Admin Services)
1.  **مدیریت پرسنل:** استخدام (افزودن)، تعدیل (حذف) یا ویرایش قرارداد کارمندان.
2.  **ثبت اسناد:** وارد کردن دستی ساعت کاری ماهانه برای پرسنل.
3.  **گزارش کل:** مشاهده مجموع بدهکار/بستانکار دانشگاه.

#### 👷 پنل کارمند (Employee Services)
1.  **فیش حقوقی خلاصه:** مشاهده دریافتی نهایی ماه جاری.
2.  **فیش حقوقی با جزئیات:** مشاهده ریز محاسبات (پایه + اضافه کار - مالیات - بیمه).
3.  **تاریخچه:** مشاهده نمودار درآمدی خود در طول سال (Line Chart).

### 🔹 مرحله چهارم: سرویس‌دهی به سایر گروه‌ها (Integration) - *Bank API*
این پروژه باید یک ماژول `integration.py` قوی داشته باشد که نقش درگاه پرداخت را ایفا کند:

1.   تابع `process_payment(student_id, amount)`:
    - این تابع توسط **پروژه غذا (۲)** و **پروژه خوابگاه (۳)** صدا زده می‌شود.
    - باید چک کند دانشجو اعتبار دارد؟ (اگر فایل کیف پول دارید).
    - اگر موفق بود، `True` برگرداند و مبلغ را کسر کند.

2.  تابع `get_faculty_salary(professor_id)`:
    - ارتباط با **پروژه ۱ (آموزش)**: تعداد واحدهای درسی استاد را از پروژه ۱ بپرسید و حق‌التدریس او را حساب کنید.

---

## 💡 راهنمایی‌های فنی
- برای محاسبه مالیات وزن‌دار: اگر لاگ کاری مربوط به ۱۰ روز است، ابتدا آن را به ۳۰ روز تعمیم دهید (ضرب در ۳)، مالیات ماهانه را حساب کنید، و سپس دوباره تقسیم بر ۳ کنید تا عدالت برقرار شود.
- حتماً تمام تراکنش‌ها (چه حقوق، چه کسر پول غذا) را در `transactions.csv` لاگ کنید تا ردیابی مالی ممکن باشد.
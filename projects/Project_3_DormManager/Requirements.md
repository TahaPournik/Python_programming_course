# پروژه ۳: سیستم مدیریت خوابگاه (DormManager)

## 🎯 هدف پروژه
مدیریت اسکان دانشجویان و ظرفیت اتاق‌ها. این سیستم باید توانایی مدیریت درخواست‌های انبوه دانشجویان برای خوابگاه را داشته باشد و بر اساس اولویت (یا زمان درخواست) اتاق‌ها را تخصیص دهد.

---

## 📂 ۱. ساختار فایل‌ها (اجباری)
```text
Project_3_DormManager/
├── data/
│   ├── login.csv           # اطلاعات ورود
│   ├── rooms.csv           # لیست اتاق‌ها (شماره، بلوک، ظرفیت کل، ظرفیت پر شده)
│   └── students/           # پوشه درخواست‌های دانشجویان
│       ├── 4016113001.csv  # وضعیت درخواست دانشجو ۱
│       └── ...
├── models/
│   ├── room.py             # کلاس اتاق
│   └── request.py          # کلاس درخواست اسکان
├── analysis/
│   └── occupancy.py        # تحلیل ظرفیت
├── reports/                # نمودارها
└── main.py                 # فایل اجرایی
```

---

## 📝 ۲. شرح وظایف (Tasks)

### 🔹 مرحله اول: مدل‌سازی (OOP)
1.  **Room**: شماره اتاق، بلوک (A, B, ...)، جنسیت (پسرانه/دخترانه)، ظرفیت، لیست ساکنین فعلی.
2.  **Request**: شماره دانشجویی، اولویت اتاق‌ها، وضعیت درخواست (در انتظار/تایید شده/رد شده).

### 🔹 مرحله دوم: منطق اسکان (Allocation Logic)
- **قانون ظرفیت:** هیچ اتاقی نباید بیش از ظرفیتش دانشجو داشته باشد.
- **قانون وضعیت:** دانشجو فقط می‌تواند یک اتاق رزرو شده داشته باشد.

### 🔹 مرحله سوم: طراحی منو (CLI Menu)

#### 👮‍♂️ پنل سرپرست خوابگاه (Admin Services)
1.  **مدیریت اتاق‌ها:** افزودن اتاق جدید، حذف اتاق (خرابی)، تغییر ظرفیت.
2.  **مدیریت درخواست‌ها:** مشاهده لیست درخواست‌های "در انتظار".
3.  **تخصیص دستی:** تایید یا رد درخواست یک دانشجو به صورت دستی.
4.  **گزارش‌گیری:**
    - مشاهده لیست کامل ساکنین یک بلوک خاص.
    - رسم نمودار میله‌ای (Bar Chart) از میزان پر بودن اتاق‌ها (چند اتاق خالی؟ چند اتاقی پر؟).

#### 🛏️ پنل دانشجو (Student Services)
1.  **مشاهده اتاق‌ها:** دیدن لیست اتاق‌های دارای ظرفیت خالی.
2.  **ثبت درخواست:** انتخاب یک اتاق و ثبت درخواست رزرو.
3.  **پیگیری:** مشاهده وضعیت درخواست (تایید شده یا رد شده).
4.  **لغو:** انصراف از خوابگاه (تخلیه اتاق).

### 🔹 مرحله چهارم: ارتباط با سایر سیستم‌ها (Integration)
1.  **احراز هویت (از پروژه ۱):**
    - دریافت اطلاعات دانشجو (نام، جنسیت، وضعیت تحصیلی) از پروژه آموزش.
    - **قانون مهم:** به دانشجویان "مشروط" یا "فارغ‌التحصیل" نباید اتاق داده شود. این وضعیت را باید از طریق تابع `integration` پروژه ۱ چک کنید.
2.  **پرداخت اجاره (به پروژه ۴) - *اختیاری*:**
    - ارسال نهایی لیست ساکنین به سیستم مالی جهت صدور فیش اجاره‌بها.

---

## 💡 راهنمایی‌های فنی
- فایل `rooms.csv` منبع اصلی حقیقت (Source of Truth) برای ظرفیت‌هاست. هر بار کسی تایید می‌شود، باید عدد "ظرفیت پر شده" در این فایل آپدیت شود.
- وضعیت درخواست در فایل شخصی دانشجو (`data/students/{id}.csv`) ذخیره شود:
  ```text
  date,room_id,status
  2023-10-01,101-A,Pending
  ```

# پروژه ۱: سیستم جامع مدیریت آموزش (EduSystem)

## 🎯 هدف پروژه
این سیستم **قلب تپنده دانشگاه** است. هدف اصلی، مدیریت اطلاعات پایه دانشجویان، اساتید و دروس است. شما باید سیستمی طراحی کنید که بر اساس **نقش کاربر** (دانشجو، استاد یا مدیر)، خدمات متفاوتی ارائه دهد.

---

## 📂 ۱. ساختار فایل‌ها (اجباری)
پوشه پروژه باید دقیقاً مطابق الگوی زیر باشد تا در سیستم تصحیح خودکار دچار مشکل نشود:

```text
Project_1_EduSystem/
├── data/
│   ├── login.csv           # اطلاعات ورود (نام کاربری، رمز عبور، نقش)
│   ├── courses.csv         # لیست دروس ارائه شده
│   ├── students.csv        # لیست کلی دانشجویان (اطلاعات پایه)
│   └── students/           # پوشه کارنامه اختصاصی دانشجویان
│       ├── 4016113001.csv  # ریز نمرات دانشجو ۱
│       ├── 4016113002.csv  # ریز نمرات دانشجو ۲
│       └── ...
├── models/
│   ├── user.py             # کلاس والد کاربر
│   ├── student.py          # کلاس دانشجو
│   ├── professor.py        # کلاس استاد
│   └── course.py           # کلاس درس
├── analysis/
│   ├── gpa_calculator.py   # محاسبات معدل
│   └── integration.py      # ماژول ارتباط با سایر گروه‌ها
├── reports/                # محل ذخیره نمودارها
└── main.py                 # فایل اصلی برنامه (منو و اجرا)
```

---

## 📝 ۲. شرح وظایف (Tasks)

### 🔹 مرحله اول: مدل‌سازی (OOP)
در پوشه `models`، کلاس‌های لازم را طراحی کنید:
1.  **User**: کلاس پایه که `username`, `password`, `role` دارد.
2.  **Student**: ارث‌بری از User. ویژگی‌ها: شماره دانشجویی، لیست دروس اخذ شده.
3.  **Professor**: ارث‌بری از User. ویژگی‌ها: کد استادی، لیست دروس ارائه شده.
4.  **Course**: نام درس، کد درس، واحد، ظرفیت.
*(نکته: در این مرحله نیازی به خواندن فایل نیست، فقط ساختار کلاس‌ها را بسازید.)*

### 🔹 مرحله دوم: مدیریت داده‌ها (Data Handling)
در فایل `main.py` یا یک ماژول کمکی:
- فایل `data/login.csv` را برای احراز هویت بخوانید.
- فایل `data/students/*.csv` را به صورت پویا بخوانید. یعنی وقتی دانشجو `401...` لاگین کرد، فقط فایل مربوط به همان دانشجو لود شود (نه همه فایل‌ها!).

### 🔹 مرحله سوم: پیاده‌سازی منطق و دسترسی‌ها (Logic & RBAC)
سیستم باید بر اساس نقش کاربر (`role` در فایل login)، دسترسی‌های زیر را محدود کند:

| نقش (Role) | دسترسی‌ها |
| :--- | :--- |
| **Admin** | دسترسی کامل (God Mode). می‌تواند هر داده‌ای را ببیند، ویرایش یا حذف کند. |
| **User (Prof)** | فقط دسترسی به دروس خودش و نمرات دانشجویان همان دروس. |
| **User (Stud)** | فقط دسترسی به اطلاعات و نمرات خودش (Read-Only) و انتخاب واحد. |

### 🔹 مرحله چهارم: طراحی منو (CLI Menu)
برنامه باید پس از لاگین، منوی متناسب با کاربر را نشان دهد:

#### 👤 پنل ادمین (Admin Services)
1.  **مدیریت دانشجویان:** افزودن، ویرایش و حذف دانشجو.
2.  **مدیریت دروس:** تعریف درس جدید، تغییر ظرفیت یا ساعت کلاس.
3.  **گزارش‌گیری:** مشاهده لیست کلی دانشجویان مشروط.

#### 🎓 پنل استاد (Professor Services)
1.  **مدیریت دروس خود:** تعریف درس جدید (با نام خودش به عنوان استاد).
2.  **مدیریت نمرات:** ثبت یا تغییر نمره دانشجویان در دروس خودش.
3.  **لیست کلاسی:** مشاهده اسامی دانشجویان ثبت‌نام کرده در کلاس‌هایش.
4.  **نمودار:** رسم نمودار توزیع نمرات برای یک درس خاص.

#### 🎒 پنل دانشجو (Student Services)
1.  **پروفایل:** مشاهده مشخصات فردی و معدل کل.
2.  **کارنامه ترم:** مشاهده نمرات ترم جاری.
3.  **کارنامه کل:** مشاهده ریز نمرات تمام ترم‌ها.
4.  **نمودار پیشرفت:** رسم نمودار خطی معدل در طول ترم‌های مختلف.

### 🔹 مرحله پنجم: ارتباط با سایر پروژه‌ها (Integration) - *امتیازی*
در فایل `analysis/integration.py`، تابعی به نام `check_student_status(student_id)` بنویسید.
- **ورودی:** شماره دانشجویی.
- **خروجی:** یک دیکشنری شامل `{'is_active': True, 'gpa': 17.5, 'name': '...'}`.
- این تابع توسط **گروه تغذیه** و **خوابگاه** import و استفاده می‌شود تا به دانشجویان غیرفعال یا مشروط سرویس ندهند.

---

## 💡 راهنمایی‌های فنی
- برای پیدا کردن فایل یک دانشجو خاص، از کتابخانه `os` استفاده کنید: `f"data/students/{student_id}.csv"`.
- برای رسم نمودارها از `matplotlib.pyplot` استفاده کنید و خروجی را در پوشه `reports` ذخیره کنید.
- رمزهای عبور را در فایل لاگین ساده ذخیره کنید (نیازی به هش کردن نیست).
